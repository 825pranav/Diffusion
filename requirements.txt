# Ingestion
aiohttp==3.14.3
aiokafka==0.14.0
python-dotenv==1.2.3

# Stream processing
spacy==3.8.16
# NER model (not on PyPI as a dependency):
#   python -m spacy download en_core_web_sm

# Database
asyncpg==0.31.0
# Embedded PostgreSQL 16 + pgvector, so local development needs no Docker.
# See scripts/devdb.py.
pgserver==0.1.4

# Machine learning
lightgbm==4.7.0
scikit-learn==1.9.1
pandas==3.0.5
numpy>=2.1,<2.8
scipy==1.18.1
joblib==1.6.0
matplotlib==3.11.1

# Agent
# In requirements-agent.txt, installed in a separate pip pass: llama-index
# declares numpy<2 and the rest of this file needs numpy>=2, which cannot be
# resolved together. See that file for why.

# API
fastapi==0.141.1
uvicorn[standard]==0.52.4
websockets==17.1
pydantic==2.13.5

# Development
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.7
